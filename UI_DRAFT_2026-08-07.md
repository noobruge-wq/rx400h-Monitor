# RX400h Monitor — 简化驾驶界面草案

日期：2026-08-07
参考：用户提供的两张 CRT / 1980s 航空电子风格草案。

## 1. 取用参考图中的部分

保留：

- 黑底、青色边框/标签、绿色主数值；
- 等宽字体与低保真 CRT/航空电子语言；
- SOC、HV Power、Speed、RPM 使用明显大于辅助信息的字号；
- 分区卡片明确，不让协议调试信息与车辆状态竞争视觉焦点；
- 最终可留一个大面积 Hybrid/Energy 区，但必须只画已经闭环的物理量/状态。

不照搬：

- 纹理噪声、扫描线、透视地板网格等纯装饰；
- 大幅汽车侧视图、写实发动机/电机图片；
- 常驻 Quick Settings 面板；
- 复杂发光和持续动画；
- 任何 raw HEX / CAN ID / request string 主界面显示。

原因是目标设备包含低性能安卓车机，界面首先要可读、稳定、低绘制负担。

## 2. Probe 清理版默认页

### 宽屏草案

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ RX400h MONITOR       ● OBDLink MX+  CONNECTED · LIVE · LOG    [设备][断开] │
│                                                        [停止实时][结束导出] │
├──────────────────────┬──────────────────────┬────────────────────────────────┤
│ VEHICLE SPEED        │ HV BATTERY           │ BATTERY POWER                  │
│                      │                      │                                │
│      63              │      58 %            │       -12.4 kW                 │
│      km/h            │                      │       CHARGE / REGEN           │
│                      │ MAX 33°C             │                                │
│                      │ MIN 30° · AVG 31.5°  │                                │
├──────────────────────┴────────────┬─────────┴────────────────────────────────┤
│ ENGINE                            │ HYBRID SYSTEM                            │
│                                  │                                          │
│ 1420 rpm       COOLANT 86°C       │ WARMUP OFF        12V OBD 13.8V          │
│ ICE RUNNING                      │ ICE +12.3  MG1 -2.4  MG2 +8.7  MGR +0.0 │
└──────────────────────────────────┴──────────────────────────────────────────┘
```

### 信息优先级

**A级：驾驶时一眼读数**

- Speed
- SOC
- HV Battery Power
- RPM

**B级：状态/热管理**

- Battery MAX temperature
- Coolant temperature
- ICE state
- Warmup state
- 12V OBD

**C级：仅辅助理解，字号最小**

- Battery MIN/AVG
- ICE/MG1/MG2/MGR signed power
- connection / LIVE / logging health

**不在主界面**

- HEX
- CAN ID
- ISO-TP frame
- command string
- latency/request stats
- T1–T8 全部温度
- Injection
- Engine Load/Timing/MAF
- Brake raw candidate

这些全部保留在日志或后续 Debug 页。

## 3. 下一阶段正式 Monitor 的 Energy 区

当 Brake overlay 与剩余状态输入闭环后，第二阶段可把下半区升级为轻量能量图：

```text
              ICE
               │ signed kW
               ▼
 MG1  ◄────── [PSD] ──────► MG2 ─────► FRONT AXLE
               ▲
               │
             HV BATTERY
               │
               └──────────► MGR ─────► REAR AXLE
```

实现原则：

- 使用 Android Canvas / Path / Text 绘制线、箭头和简化图标；
- 不使用写实 PNG 部件，不做持续粒子动画；
- 方向由已经闭环的 signed component power / state 决定；
- 数据 unavailable 时明确显示 `— / UNKNOWN`，不自行推断；
- Brake overlay 没闭环前不画“确定”的制动能量箭头。

## 4. 交互原则

主页面不是开发控制台。正常驾驶时最多需要：

- 连接状态；
- 开始/停止实时；
- 结束并导出。

设备选择属于低频操作，可以保留在顶部小按钮；未来正式 Monitor 可进一步收入 Settings。

原始协议诊断若未来仍需人工查看，应放进独立 Debug 页面，并默认关闭。
