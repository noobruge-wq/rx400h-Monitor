# V0.1.10 Cleanup Candidate — Change Log

基线：V0.1.8
V0.1.9：历史候选，未作为合并基线。

## UI

- 默认驾驶可读 Dashboard 替代 dataText + rawText 开发控制台。
- 移除主界面 HEX/CAN/请求滚动显示。
- 移除链路确认、HA链验证、六个手动事件按钮。
- 主界面突出 Speed / SOC / HV Power / RPM / temperature / coolant / 12V / warmup。

## Runtime model / parser

- 删除错误 `61C3 d[8:10]/100 = ICE Power`。
- `21CDF3` 使用 ICE Torque + Injection µL 已恢复语义；Injection 不作为燃烧判据。
- `21C4` 加入 verified Warmup Active bit。
- 保存 Engine Load / Timing / MAF。
- 保存 Brake Regen / Master Torque candidate，暂不用于驾驶 UI。
- Response Pending 改为结构化 payload 起点判断。

## Dead code / log cleanup

- 删除 ProtocolAttempt / bestProtocol / protocol_matrix 与对应 logger API。
- 删除 SignalValue.rawResponse。
- 移除多个无消费 typed decoder 字段；raw response 仍完整保留。
- AT 文本统计分类修正。
- adapter MAC 改哈希标识。
- FileProvider 增加 internal files fallback。
- Bluetooth adapter 获取改为 BluetoothManager。

## Deliberately unchanged

- 车辆请求集合；
- V0.1.8 scheduler/request timing；
- 严格单事务；
- Logger finalize state machine；
- 协议 Header 7E0/7E2；
- 无任意命令输入。
